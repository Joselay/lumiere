{
  "id": "310f1e17",
  "title": "Improve execution quality and cost controls",
  "tags": [
    "profitability",
    "execution",
    "risk"
  ],
  "status": "closed",
  "created_at": "2026-06-24T01:25:06.883Z"
}

## Completed
- Added pre-trade expected-edge vs estimated total execution cost guard in risk; low-edge trades are rejected with `expected_edge_below_cost` and counted.
- Strategy decisions now emit `expected_edge_bps`; account snapshots can carry spread, estimated depth slippage, total cost, realized slippage, and cost-rejection counters.
- OKX order-book quality now estimates executable depth slippage from multiple book levels, not just top-of-book spread.
- Backtest and paper mode continue using the shared `CostModel`, aligning modeled fee/spread/slippage assumptions with live risk inputs.
- Engine logs/performance text include expected edge, estimated cost/slippage, realized slippage, and rejected-by-cost counts.
- Added configurable `OKX_ORDER_TYPE` so marketable-limit experiments can be routed through existing order placement with limit prices.

## Verification
- `uv run ruff check .`
- `uv run pytest`
