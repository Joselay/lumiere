{
  "id": "a985f1d4",
  "title": "Add regime filters and non-MA strategy candidates",
  "tags": [
    "profitability",
    "strategy",
    "research"
  ],
  "status": "closed",
  "created_at": "2026-06-24T01:24:54.219Z"
}

## Completed
- Added a pluggable `TradingStrategy` interface and strategy factory so engine/backtest plumbing can run non-MA strategies.
- Added two new strategy modules: `rsi_mean_reversion` and `volatility_breakout` with unit coverage.
- Added regime metadata and decision inputs for ranging/trending, volatility, and spread/liquidity suitability; backtest report parameters include `allowed_regimes`.
- Added config/CLI strategy selection via `STRATEGY_NAME` and `lumiere-backtest --strategy` without code edits.
- Updated README and `.env.example` with selectable strategy settings.

## Verification
- `uv run ruff check .`
- `uv run pytest`
- `uv run lumiere-backtest --help | rg -- '--strategy|rsi|breakout'`
