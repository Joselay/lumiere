# AGENTS.md

For OKX API work, use `python-okx==0.4.1` instead of raw API calls. Use the local cached `python-okx` docs/source at `~/.cache/checkouts/github.com/okxapi/python-okx` when needed.

For OKX CLI/manual operations, use the local cached `okx/agent-skills` docs at `~/.cache/checkouts/github.com/okx/agent-skills` and follow the relevant skill instructions.
